* Sort out bench (standard suite into C++ executable) (JS)
  - keep generic functionality 
  - delete runBench.sh

* create table with JSON/Jason size comparisons (JS)

* properly test custom types 0xf0 - 0xff (JS)
  - Builder already has support for them
  - Dumper & Slicer need support
  - tests

* implement lookup table support in Builder, Slice, Parser and Dumper (MN)

* support modification of Jason objects (removing, updating) (JS)
  - JasonHelper class
  - move JasonSlice::iterate into JasonHelper class

* documentation of C++ classes (// ...) (MN)

* test on MacOS, ARM and Windows

* run tests on ARM again

* comparison with other formats

* add performance tests and results

* create code examples for documentation purposes

* implement type BCD in Builder, Slice, Parser and Dumper

* SWIG support (MN)

